import sys; import os; sys.path.append(os.path.dirname(os.path.abspath(__file__))); from app.core.database import SessionLocal; from app.crud.employee import create_employee_quick; from app.models.employee import Employee; db = SessionLocal(); emp = create_employee_quick(db, 'Test Python User 2'); emps = db.query(Employee).order_by(Employee.id.desc()).limit(5).all(); print([(e.id, e.full_name) for e in emps])  
